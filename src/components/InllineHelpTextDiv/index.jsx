import styled from "styled-components";

export default styled.div`
  font-size: 12px;
  display: inline;
  padding-left: 10px;
  position: relative;

  > i {
    position: absolute;
    top: -8px;
  }
`;
