/* eslint-disable import/no-anonymous-default-export */
import React from "react";
import styled from "styled-components";

export default () => {
  return <Wrapper>Pairwise combinations</Wrapper>;
};

const Wrapper = styled.div``;
